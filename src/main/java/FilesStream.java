import java.io.File;
import java.io.IOException;
import java.nio.file.Files;
import java.nio.file.Paths;
import java.util.stream.Stream;

public class FilesStream {

//    public void getFileList() throws IOException {
//        //Files.list(Paths.get("src/main/java")).forEach(System.out::println);
//        Files.newDirectoryStream(Paths.get("src/main/java"),path->path.toString().endsWith(".java")).forEach(System.out::println);
//        Stream.of(new File("src/main")
//    }
}
